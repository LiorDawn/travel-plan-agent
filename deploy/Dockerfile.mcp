FROM python:3.11-slim
WORKDIR /app
COPY mcp_servers/ /app/mcp_servers/
RUN pip install --no-cache-dir fastapi uvicorn pydantic
EXPOSE 8100
CMD ["python", "-m", "uvicorn", "mcp_servers.weather_server:app", "--host", "0.0.0.0", "--port", "8100"]
